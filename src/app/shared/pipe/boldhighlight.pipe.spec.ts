import { BoldhighlightPipe } from './boldhighlight.pipe';

describe('BoldhighlightPipe', () => {
  it('create an instance', () => {
    const pipe = new BoldhighlightPipe();
    expect(pipe).toBeTruthy();
  });
});
